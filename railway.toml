[build]
builder = "nixpacks"

[deploy]
startCommand = "PYTHONPATH=. python backend/main.py"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
